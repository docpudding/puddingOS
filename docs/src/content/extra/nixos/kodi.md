This module provides a simple way to use and configure the Kodi media player. In addition to installing the packages you would need to run it normally, it also provides a custom `startkodi` command, which can be used to launch Kodi in a standalone X session in its own TTY.

It is compatible with the `pos.sessions.autostart` system, which can be used to automatically start Kodi in a specific TTY.
