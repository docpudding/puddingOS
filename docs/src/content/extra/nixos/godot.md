This module provides access to Godot Engine for your environment. It generally uses more recent versions than what is provided in stable nixpkgs. In addition to providing access to both the standard version and the Mono version of the engine, it also provides the [Voxel Tools](https://voxel-tools.readthedocs.io/en/latest/getting_the_module/) custom build as well as an optional interface for remote debugging.
